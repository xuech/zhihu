import { createStore } from 'vuex'
import { testData } from '../mock/testData'

export default createStore({
  state: {
    columns: testData
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
